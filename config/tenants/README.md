# Tenants

Tenant onboarding templates live in this directory. The default template is used by automated
provisioning workflows to scaffold tenant resources.

- `default.yaml`: baseline tenant template applied during onboarding.
