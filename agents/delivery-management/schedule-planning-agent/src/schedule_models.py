"""
Pydantic models for the Schedule Planning Agent.

Note: The domain event models (ScheduleBaselineLockedEvent, ScheduleDelayEvent)
are defined externally in the contracts package and imported where needed.
This module is reserved for any schedule-specific Pydantic models that may be
introduced as the agent evolves from plain dict-based data structures.
"""
