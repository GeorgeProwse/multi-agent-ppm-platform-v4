"""
Quality Management Agent - Action handlers package.

Each module contains action handler functions extracted from the monolithic
QualityManagementAgent class.  Every handler accepts the agent instance as
its first argument so it can access stores, configuration, and shared helpers.
"""
