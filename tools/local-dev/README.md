# Local Development

Use the scripts in this folder to spin up the local developer environment.

## Services

- API Gateway (`api`) on `http://localhost:8000`
- Web Console (`web`) on `http://localhost:8501`
- Redis + Postgres for local persistence

## Commands

```bash
bash tools/local-dev/dev_up.sh
bash tools/local-dev/dev_down.sh
```
