version: 1
thresholds:
  mappings:
    rule_id: missing_manifest_mappings
    max_missing_connectors: 0
  tier1:
    max_business_priority: 5
    required_capabilities:
      - read
      - write
      - webhook
# Exceptions are now defined in each connector's manifest.yaml under
# maturity.exceptions.  This central file retains only global thresholds.
exceptions: []
